# Backend configuration (.env)
# Copy this to backend/.env and adjust values as needed.

# Enable Ollama by default (UI toggle can override per-message)
OLLAMA_ENABLED=true

# Ollama connection
OLLAMA_HOST=127.0.0.1
OLLAMA_PORT=11434
OLLAMA_MODEL=chatgpt-oss:20b
OLLAMA_TIMEOUT_SECONDS=60
